
bits 16



; mov bx, 5
; add bx, 7
; 
; mov ax, 244
; add ax, 100
; add ax, 1024
; 
; add bx, 7
; 
; sub ax, 1024
; sub ax, 100
; sub ax, 256

; add ax, 100


; mov bx, -4093
; mov cx, 3841
; sub bx, cx
; 
; mov sp, 998
; mov bp, 999
; cmp bp, sp
; 
; add bp, 1027
; sub bp, 2026

add bx, 30000
add bx, 10000
sub bx, 5000
sub bx, 5000

mov bx, 1
mov cx, 100
add bx, cx

mov dx, 10
sub cx, dx

add bx, 40000
add cx, -90

mov sp, 99
mov bp, 98
cmp bp, sp

; mov ax, -10
; add ax, 10

mov ax, 10
sub ax, 20

mov ax, 14
add ax, 3
